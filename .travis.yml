language: csharp

matrix:
  include:
    - os: linux # Ubuntu 14.04
      dist: trusty
      sudo: required
      mono: none
      dotnet: 1.0.1

notifications:
  email: false

branches:  
  only:
    - master

before_install:  
  - chmod +x build.sh

script:  
  - ./build.sh